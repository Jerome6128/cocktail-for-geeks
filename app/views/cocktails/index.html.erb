
<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= image_path 'cocktails.jpg' %>');">
  <div class="banner-content">
    <h1>Cocktail for geeks</h1>
    <p>Awesome beverages for developers only && by developers only :-)</p>
    <%= link_to 'Create a new cocktail', new_cocktail_path, class: "btn btn-primary btn-lg" %>
  </div>
</div>

<h3>Cocktails validated by geeks</h1>
<ul class="list-group">
  <% @cocktails.each do |cocktail| %>
    <li class="list-group-item"><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
  <% end %>
</ul>
